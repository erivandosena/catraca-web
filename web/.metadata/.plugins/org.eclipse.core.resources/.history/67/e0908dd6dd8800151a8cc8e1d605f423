<?php


class VinculoDAO extends DAO{
	
	/**
	 * Pega-se o Id da base do SIG. Antes vamos verificar se existe na base local. 
	 * Se existe щ mais fсcil. Vamos para o Passo adicionar Vinculo.
	 * 
	 * Se nуo existir, pegamos da base do SIG e adicionamos na base local. Indo para o passo de adicionar vinculo. 
	 * 
	 * Adicionar Vinculo
	 *
	 * Vai ser o seguinte, acabamos de adicionar um usuario na base local. Utilizaremos o id inserido. 
	 * Iremos adicionar o vinculo para esse usuario com dados de cartao e vinculo. 
	 * 
	 * 
	 * Mas antes nуo sabemos se o cartуo jс щ cadastrado. 
	 * Caso o cartуo seja cadastrado, fazemos um Update para que o tipo seja adequado a este. 
	 * Caso contrario, cadastramos o cartуo. 
	 * Ao cadastrar o cartуo ainda precisamos do ID do cartуo.  
	 * Temos o ID do usuario adicionado e o ID do cartуo adicionado. Faчamos adiчуo de vinculo. 
	 * 
	 * Opa, mas tem algo que deve ser visto. Se o cartуo ja estava cadastrado e tinha vinculo? 
	 * Temos que alterar esse vinculo para que deixe de valer a partir de agora, e entуo continuar o processo. ]
	 * 
	 * Mais uma coisa, se o usuario jс existia cadastrado e tambщm tinha cartуo vinculado a ele. 
	 * Algum nуo era avulso? Faчa o processo de substituiчуo de vinculo. Vinculo de cartуo anterior 
	 * щ atualizado para que tenha vinculo vencido e o novo vinculo serс finalmente adicionado. 
	 * 
	 *  Caso vinculo seja avulso nуo se leve em consideraчуo vinculos do usuario. 
	 *  

	 * @param int $idUsuarioBaseExterna
	 * @param Vinculo $vinculo
	 */
	public function adicionaVinculo($idUsuarioBaseExterna, Vinculo $vinculo){
		
		$idUsuarioBaseExterna = intval($idUsuarioBaseExterna);
		
		
		
	}
	
	
}

?>